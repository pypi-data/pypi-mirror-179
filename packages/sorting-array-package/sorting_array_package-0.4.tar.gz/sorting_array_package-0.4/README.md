# Практическая работа №6 – Модульные программы

**Савельев Александр**,
**КИ22-16/1Б**,
**Вариант 28**

`$ python package --help` — инфо о пакете

`$ python package --test` — вызов тестов

`$ python package --list 9 8 7 6 0 ` — сортировка списка [9, 8, 7, 6, 0]

`$ python package --random 5 -100 100` — сортировка списка размером 5 заполненного случайными числами в диапозоне от -100 до 100

