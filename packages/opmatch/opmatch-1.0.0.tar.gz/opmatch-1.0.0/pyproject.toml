[build-system]
requires = [
    "setuptools>=42",
    "wheel",
    "certifi",
    "pandas",
    "scikit-learn",
    "scipy",
    "numpy",
    "matplotlib"
]
build-backend = "setuptools.build_meta"