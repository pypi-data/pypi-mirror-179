[tool.poetry]
name = "private-reg"
version = "0.1.0"
description = "A siple pytho program for managing container registry in private"
authors = ["selva <selva.g@subcom.tech>"]
readme = "README.md"
packages = [{include = "src/private_reg"}]

[tool.poetry.dependencies]
python = "^3.10"
docker = "^6.0.1"
typer = "^0.7.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
