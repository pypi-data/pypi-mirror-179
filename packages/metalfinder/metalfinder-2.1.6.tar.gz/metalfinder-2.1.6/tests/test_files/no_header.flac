This is not really a FLAC file, but mutagen sees the ".flac" extension, sees it
is not empty, so it looks for the FLAC headers and can't find it. It should then
error with mutagen.flac.FLACNoHeaderError :)
