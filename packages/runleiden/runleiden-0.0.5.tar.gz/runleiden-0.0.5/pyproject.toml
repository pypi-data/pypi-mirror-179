[tool.poetry]
name = "runleiden"
version = "0.0.5"
description = "Wrapper over leidenalg"
authors = ["runeblaze <runeblaze@excite.co.jp>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.7"
leidenalg = "^0.9.0"
typer = "^0.6.1"
structlog = "^22.1.0"

[tool.poetry.scripts]
runleiden = 'runleiden.runleiden:entry_point'

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
