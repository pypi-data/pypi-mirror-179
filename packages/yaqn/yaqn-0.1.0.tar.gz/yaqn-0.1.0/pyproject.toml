[tool.poetry]
name = "yaqn"
version = "0.1.0"
description = "A quicknote app made in python and tkinter."
authors = ["kutu-dev"]
license = "MIT License"
readme = "README.md"

[tool.poetry.scripts]
yaqn = "yaqn.main:main"

[tool.poetry.dependencies]
python = "^3.11"

[tool.poetry.group.dev.dependencies]
mypy = "^0.991"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
